# Start  JSON Server

1:Install JSON Server
#  npm install -g json-server

2:Start JSON Server
# json-server --watch db.json --port 5000

--------------------------------------------------

# npm install
# npm start
